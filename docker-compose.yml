services:
  minbot:
    build: .
    env_file:
      - ~/.minbot/.env
    volumes:
      - ~/.minbot:/root/.minbot
      - ~/.claude:/root/.claude
      - workspace:/workspace
    restart: unless-stopped

volumes:
  workspace:
